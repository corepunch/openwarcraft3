#include "s_skills.h"

// Disabled until patrol owns a custom stand move; Linux -Wall warns on unused static hooks.
// static umove_t patrol_stand = { "stand", ai_stand, NULL, &a_patrol };

ability_t a_patrol = {
};
