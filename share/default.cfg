// OpenWarcraft3 default cvars.
// Loaded first; build-specific defaults, the generated config, and autoexec can override these.

seta data ""
seta map ""
seta connect ""
seta r_module "renderer"
seta ui_module "ui"
seta g_module "game"
set com_frame_limit "0"
