// OpenWarcraft3 gameplay defaults.
// User overrides are written to share/openwarcraft3-config.cfg.

bind MOUSE1 "+select"
bind MOUSE2 "+smart"
bind MOUSE3 "+pan"
bind ALT+MOUSE1 "+pan"
bind q "cmd quests"
bind ESCAPE "cmd cancel"
