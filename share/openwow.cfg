// OpenWoW gameplay defaults.
// User overrides are written to share/openwow-config.cfg.

bind MOUSE1 "+wowattack"
bind MOUSE2 "+look"
bind w "+forward"
bind s "+back"
bind a "+moveleft"
bind d "+moveright"
bind ESCAPE "quit"
